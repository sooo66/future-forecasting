# 核心依赖
requests>=2.31.0
pandas>=2.0.0
polars>=0.19.0  # 可选，用于高性能数据处理

# 异步爬虫
crawl4ai>=0.3.0
aiohttp>=3.9.0

# 数据处理
beautifulsoup4>=4.12.0
lxml>=4.9.0
pyarrow>=14.0.0  # Parquet 支持

# URL 处理
tldextract>=5.0.0
urllib3>=2.0.0

# 配置管理
tomli>=2.0.0; python_version < "3.11"
tomli-w>=1.0.0  # 用于写入 TOML（测试需要）

# 日志
loguru>=0.7.0

# 进度条
tqdm>=4.66.0

# 代理支持
freeproxy>=1.0.0  # 可选，用于代理支持

# 语言检测（可选，如果需要更精确的语言检测）
# langdetect>=1.0.9
# fasttext>=0.9.2

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0

# 日期处理
python-dateutil>=2.8.0

